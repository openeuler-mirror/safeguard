.TH SAFEGUARD 8 "2026-07-22" "safeguard 3.0" "System Administration"
.SH NAME
safeguard \- Linux 安全审计与白名单控制器（基于 eBPF/LSM）
.SH SYNOPSIS
.B safeguard
.RB [ \--config
.IR FILE ]
.RB [ \--version ]
.RB [ \--help ]
.sp
.B safeguard controller generate
.RB [ \--output
.IR PATH ]
.RB [ \--report
.IR PATH ]
.RB [ \--mode
.IR MODE ]
.SH DESCRIPTION
.B safeguard
是基于 eBPF 和 Linux Security Module (LSM) 的主机安全审计与控制工具。
它通过在内核 LSM 钩子上挂载 eBPF 程序，实现对网络、文件、挂载和进程四类安全事件的
实时监测与拦截。
.sp
每个审计模块支持两种运行模式：
.sp
.RS 4
.TP 8
.B monitor
仅记录事件日志，不阻断操作。适合策略验证阶段。
.TP
.B block
拦截匹配策略的操作，并记录日志。适合生产环境。
.RE
.sp
每个模块支持两种作用范围：
.sp
.RS 4
.TP 8
.B host
全局生效，包括主机和容器内所有进程。
.TP
.B container
仅对容器内进程生效，主机进程不受限制。
.RE
.sp
全局策略支持两种模式：
.sp
.RS 4
.TP 8
.B blacklist
（默认）除明确拒绝的条目外全部允许。
.TP
.B whitelist
除明确允许的条目外全部拒绝。推荐用于高安全场景。
.RE
.sp
.B safeguard
必须以 root 用户运行，且内核须启用 BPF LSM 和 BTF。
.SH OPTIONS
.SS "全局选项"
.TP
.BR \--config \ FILE
指定 YAML 配置文件路径。默认值：\fIsafeguard.yaml\fP。
也可通过环境变量 \fBSG_CONFIG_PATH\fP 设置。
.TP
.BR \--version
显示版本号。
.TP
.BR \--help
显示帮助信息。
.SS "controller generate 子命令选项"
.TP
.BR \--output \ PATH
白名单 YAML 配置文件的输出路径。默认值：\fIdemo-whitelist.yaml\fP。
.TP
.BR \--report \ PATH
白名单 JSON 报告文件的输出路径。默认值：\fIdemo-whitelist-report.json\fP。
.TP
.BR \--mode \ MODE
生成配置的操作模式：\fImonitor\fP（仅日志）或 \fIblock\fP（强制执行）。
默认值：\fImonitor\fP。
.SH CONFIGURATION
配置文件为 YAML 格式，包含以下顶层字段：
.sp
.B policy
.RS 4
全局策略模式。\fIblacklist\fP（默认允许）或 \fIwhitelist\fP（默认拒绝）。
.RE
.sp
.B network
.RS 4
网络访问控制配置。子字段：
.TP
.B enable
是否启用。默认 \fIfalse\fP。
.TP
.B mode
\fImonitor\fP 或 \fIblock\fP。
.TP
.B target
\fIhost\fP 或 \fIcontainer\fP。
.TP
.B cidr
IP 地址段控制。包含 \fIallow\fP 和 \fIdeny\fP 列表，每项为 CIDR 字符串（如 \fI10.0.0.0/8\fP）。
.TP
.B domain
域名控制。包含 \fIallow\fP 和 \fIdeny\fP 列表。域名解析依赖 \fBdns_proxy\fP 或周期性异步解析。
.TP
.B command
进程命令名控制。包含 \fIallow\fP 和 \fIdeny\fP 列表，匹配进程的 comm 字段（如 \fIcurl\fP）。
.TP
.B uid
用户 ID 控制。包含 \fIallow\fP 和 \fIdeny\fP 列表。
.TP
.B gid
组 ID 控制。包含 \fIallow\fP 和 \fIdeny\fP 列表。
.RE
.sp
.B files
.RS 4
文件访问控制配置。子字段：
.TP
.B enable
是否启用。默认 \fIfalse\fP。
.TP
.B mode
\fImonitor\fP 或 \fIblock\fP。
.TP
.B target
\fIhost\fP 或 \fIcontainer\fP。
.TP
.B allow
允许访问的路径列表（whitelist 模式下使用）。路径须为绝对路径。
.TP
.B deny
拒绝访问的路径列表（blacklist 模式下使用）。
.RE
.sp
.B mount
.RS 4
挂载操作控制配置。子字段：
.TP
.B enable
是否启用。默认 \fIfalse\fP。
.TP
.B mode
\fImonitor\fP 或 \fIblock\fP。
.TP
.B target
\fIhost\fP 或 \fIcontainer\fP。
.TP
.B deny
拒绝的挂载源路径或设备名列表（如 \fI/var/run/docker.sock\fP）。
.RE
.sp
.B process
.RS 4
进程执行控制配置。子字段：
.TP
.B enable
是否启用。默认 \fIfalse\fP。
.TP
.B mode
\fImonitor\fP 或 \fIblock\fP。
.TP
.B target
\fIhost\fP 或 \fIcontainer\fP。
.TP
.B allow
允许执行的进程名列表（whitelist 模式下使用），匹配可执行文件的 basename（如 \fIbash\fP、\fIpython3\fP）。
.sp
注意：进程拦截当前仅在全局策略为 \fIwhitelist\fP 时生效。
.RE
.sp
.B dns_proxy
.RS 4
DNS 代理配置。启用后 safeguard 启动 DNS 代理服务，
拦截域名解析请求，对受限域名直接阻断，其他请求转发到上游 DNS。
.TP
.B enable
是否启用。默认 \fIfalse\fP。
.TP
.B upstreams
上游 DNS 服务器列表。启用时必填（如 \fI8.8.8.8\fP、\fI8.8.4.4\fP）。
.TP
.B bind
DNS 代理监听地址列表。默认值：\fI127.0.0.1\fP、\fI172.17.0.1\fP（Docker 默认桥接地址）。
.RE
.sp
.B log
.RS 4
日志配置。子字段：
.TP
.B level
日志级别：\fIDEBUG\fP、\fIINFO\fP、\fIWARN\fP、\fIERROR\fP。默认 \fIINFO\fP。
.TP
.B format
日志格式：\fIjson\fP 或 \fItext\fP。默认 \fIjson\fP。
.TP
.B output
日志输出路径。默认 \fIstdout\fP。可指定文件路径（如 \fI/var/log/safeguard.log\fP）。
.TP
.B max_size
日志轮转大小上限（MB）。默认 100。
.TP
.B max_age
日志保留天数。默认 365。
.TP
.B labels
附加到每条日志的键值标签。
.RE
.SH EXAMPLES
.SS "黑名单模式：拒绝特定文件访问"
.EX
sudo safeguard --config /etc/safeguard/safeguard.yml
.EE
.sp
配置示例：
.EX
files:
  enable: true
  mode: block
  target: host
  deny:
    \- /etc/passwd
    \- /etc/shadow
.EE
.SS "白名单模式：只允许指定进程执行"
.EX
sudo safeguard --config /etc/safeguard/whitelist.yaml
.EE
.sp
配置示例：
.EX
policy: whitelist
process:
  enable: true
  mode: block
  target: host
  allow:
    \- bash
    \- sh
    \- python3
.EE
.SS "阻止容器挂载 Docker socket"
.EX
mount:
  enable: true
  mode: block
  target: host
  deny:
    \- /var/run/docker.sock
.EE
.SS "阻止 SSRF 攻击（拦截云元数据服务）"
.EX
network:
  enable: true
  mode: block
  target: host
  cidr:
    allow:
      \- 0.0.0.0/0
    deny:
      \- 169.254.169.254/32
.EE
.SS "容器内阻止特权逃逸路径"
.EX
files:
  enable: true
  mode: block
  target: container
  allow:
    \- /
  deny:
    \- /proc/sysrq-trigger
    \- /sys/kernel
    \- /proc/sys/kernel
.EE
.SS "生成白名单配置"
.EX
sudo safeguard controller generate \--output /etc/safeguard/whitelist.yaml \--mode block
.EE
.SS "生成白名单后立即应用"
.EX
sudo safeguard controller generate \--output whitelist.yaml && sudo safeguard \--config whitelist.yaml
.EE
.SS "启用 DNS 代理模式"
.EX
network:
  enable: true
  mode: block
  target: host
  cidr:
    allow:
      \- 0.0.0.0/0
  domain:
    deny:
      \- example.com
dns_proxy:
  enable: true
  upstreams:
    \- 8.8.8.8
    \- 8.8.4.4
  bind:
    \- 127.0.0.1
    \- 172.17.0.1
.EE
.sp
启用 \fBdns_proxy\fP 后，须将系统 DNS 指向 \fBdns_proxy.bind\fP 地址：
.EX
# 修改 /etc/resolv.conf
nameserver 127.0.0.1
nameserver 172.17.0.1
.EE
.sp
若使用 systemd-resolved，修改 \fI/etc/systemd/resolved.conf\fP：
.EX
[Resolve]
DNS=127.0.0.1 172.17.0.1
.EE
然后执行 \fBsystemctl restart systemd-resolved\fP。
